# 🚀 CodeSnippet Hub - 完整部署指南

> **👋 欢迎！这是一份为新手准备的超详细部署教程**  
> 即使你是第一次接触全栈项目，跟着本指南也能成功运行！

---

## 📋 目录

- [环境准备](#环境准备)
- [快速开始](#快速开始)
- [详细步骤](#详细步骤)
- [常见问题](#常见问题)
- [功能测试](#功能测试)
- [项目清理](#项目清理)

---

## 🛠️ 环境准备

### 必需软件清单

在开始之前，请确保你的电脑已安装以下软件：

#### 1️⃣ Node.js (v16+)

**作用：** JavaScript运行环境，用于运行前端和后端代码

**下载地址：** https://nodejs.org/

**安装验证：**
```bash
node --version  # 应显示 v16.x.x 或更高
npm --version   # 应显示 8.x.x 或更高
```

---

#### 2️⃣ MySQL (v8.0+)

**作用：** 数据库，存储用户和代码片段数据

**下载地址：** https://dev.mysql.com/downloads/mysql/

**安装提示：**
- 记住你设置的 **root 密码**（非常重要！）
- 默认端口：`3306`

**安装验证：**
```bash
mysql --version  # 应显示 MySQL 8.x
```

---

#### 3️⃣ Docker Desktop

**作用：** 容器化工具，用于运行Redis缓存

**下载地址：** https://www.docker.com/products/docker-desktop/

**安装验证：**
```bash
docker --version  # 应显示 Docker version 20.x.x 或更高
```

**注意：** 安装后需要启动 Docker Desktop 应用程序！

---

#### 4️⃣ Git

**作用：** 版本控制工具，用于下载项目代码

**下载地址：** https://git-scm.com/

**安装验证：**
```bash
git --version  # 应显示 git version 2.x.x
```

---

### 推荐工具（可选）

- **VS Code** - 代码编辑器：https://code.visualstudio.com/
- **Postman** - API测试工具：https://www.postman.com/
- **DataGrip** - 数据库管理工具：https://www.jetbrains.com/datagrip/

---

## ⚡ 快速开始

### 一键部署（5分钟）

如果你的环境已经配置好，可以使用以下快速命令：

```bash
# 1. 克隆项目
git clone https://github.com/michael123610/codesnippet-hub.git
cd codesnippet-hub

# 2. 启动 Docker Redis
docker run -d --name codesnippet-redis -p 6379:6379 redis

# 3. 配置数据库（见下文）

# 4. 启动后端（新终端）
cd backend
npm install
npm start

# 5. 启动前端（新终端）
cd frontend
npm install
npm run dev
```

**访问：** http://localhost:5173

---

## 📖 详细步骤

### 第一步：下载项目代码

#### 方法1：使用 Git（推荐）

```bash
# 在你想要存放项目的目录下执行
git clone https://github.com/michael123610/codesnippet-hub.git

# 进入项目目录
cd codesnippet-hub
```

#### 方法2：下载 ZIP

1. 访问：https://github.com/michael123610/codesnippet-hub
2. 点击绿色按钮 "Code"
3. 选择 "Download ZIP"
4. 解压到你想要的目录（**建议：** `D:\codesnippet-hub`）

---

### 第二步：启动 Docker Redis

Redis 用于缓存热门代码片段，提升性能。

**Windows CMD 或 PowerShell：**
```bash
docker run -d --name codesnippet-redis -p 6379:6379 redis
```

**验证 Redis 是否启动：**
```bash
docker ps
```

你应该能看到一个名为 `codesnippet-redis` 的容器在运行！

---

### 第三步：配置 MySQL 数据库

#### 1️⃣ 创建数据库

**方法A：使用命令行**

```bash
# 登录 MySQL（输入你的 root 密码）
mysql -u root -p

# 执行以下 SQL 命令
CREATE DATABASE IF NOT EXISTS codesnippet_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 验证数据库已创建
SHOW DATABASES;

# 退出
EXIT;
```

**方法B：使用图形化工具（如 DataGrip）**

1. 打开 DataGrip
2. 连接到你的 MySQL 服务器
3. 右键 → New → Database
4. 数据库名：`codesnippet_db`
5. 字符集：`utf8mb4`

---

#### 2️⃣ 配置后端数据库连接

**编辑 `backend/config/database.js` 文件：**

```javascript
module.exports = {
  host: 'localhost',
  user: 'root',
  password: '你的MySQL密码',  // ⚠️ 改成你的密码！
  database: 'codesnippet_db',
  port: 3306
};
```

**⚠️ 重要提示：**
- 将 `你的MySQL密码` 替换为你安装 MySQL 时设置的 root 密码
- 如果密码有特殊字符，用引号包裹：`'my@pass#123'`

---

### 第四步：启动后端服务器

**打开新的终端（CMD 或 PowerShell）：**

```bash
# 进入后端目录
cd backend

# 安装依赖（第一次运行需要，大约1-3分钟）
npm install

# 启动后端服务器
npm start
```

**成功标志：**
```
✅ MySQL connected successfully!
✅ Redis connected successfully!
🚀 Server running on http://localhost:3000
```

**⚠️ 不要关闭这个终端窗口！** 让它一直运行。

---

### 第五步：启动前端开发服务器

**再打开一个新的终端：**

```bash
# 进入前端目录
cd frontend

# 安装依赖（第一次运行需要，大约1-3分钟）
npm install

# 启动前端开发服务器
npm run dev
```

**成功标志：**
```
  VITE v4.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

---

### 第六步：访问应用

**在浏览器打开：**
```
http://localhost:5173
```

你应该能看到 CodeSnippet Hub 的首页！🎉

---

## 🧪 功能测试

### 测试账号登录

**默认测试账号：**
- 📧 邮箱：`demo@codesnippet.com`
- 🔑 密码：`demo123`

### 注册新账号

1. 点击右上角 "注册"
2. 填写信息：
   - 用户名：随意（例如：`test_user`）
   - 邮箱：随意（例如：`test@example.com`）
   - 密码：至少6位（例如：`123456`）
3. 点击 "注册"

### 创建代码片段

1. 登录后，点击 "创建代码"
2. 填写：
   - 标题：例如 `Hello World`
   - 选择语言：JavaScript
   - 输入代码：`console.log("Hello World!");`
   - 添加标签：`JavaScript`, `基础`
3. 点击 "创建"

### 测试点赞和收藏

1. 在首页浏览代码片段
2. 点击代码卡片进入详情
3. 点击 ❤️ 点赞
4. 点击 📑 收藏
5. 去 "我的收藏" 查看

### 多用户互动测试

1. 用一个账号创建代码片段
2. 退出登录
3. 注册另一个账号
4. 在首页看到第一个账号的代码
5. 点赞和收藏
6. 切回第一个账号，看到点赞数增加

---

## ❓ 常见问题

### Q1: `npm install` 卡住不动？

**解决方法：** 切换国内镜像源

```bash
# 切换到淘宝镜像
npm config set registry https://registry.npmmirror.com

# 再次尝试安装
npm install
```

---

### Q2: MySQL 连接失败？

**错误信息：**
```
Error: ER_ACCESS_DENIED_ERROR
```

**解决方法：**
1. 检查 `backend/config/database.js` 中的密码是否正确
2. 确认 MySQL 服务正在运行：
   ```bash
   # Windows
   net start MySQL80
   
   # 或在任务管理器中查看 MySQL 服务
   ```

---

### Q3: Redis 连接失败？

**错误信息：**
```
Error: Redis connection failed
```

**解决方法：**
1. 确认 Docker Desktop 正在运行
2. 检查 Redis 容器状态：
   ```bash
   docker ps
   ```
3. 如果没有看到 `codesnippet-redis`，重新启动：
   ```bash
   docker run -d --name codesnippet-redis -p 6379:6379 redis
   ```

---

### Q4: 端口被占用？

**错误信息：**
```
Port 3000 is already in use
```

**解决方法：**

**Windows：**
```bash
# 查找占用端口的进程
netstat -ano | findstr :3000

# 杀死进程（替换 PID 为上面查到的进程ID）
taskkill /PID <PID> /F
```

**或者修改端口：**

编辑 `backend/server.js`：
```javascript
const PORT = 3001;  // 改成其他端口
```

---

### Q5: 前端页面空白？

**可能原因：**
- 后端未启动
- 浏览器缓存问题

**解决方法：**
1. 确认后端运行在 `http://localhost:3000`
2. 清除浏览器缓存（Ctrl + Shift + Delete）
3. 强制刷新（Ctrl + F5）

---

### Q6: 创建代码片段失败？

**检查项：**
1. 是否已登录？
2. 后端是否正常运行？
3. 打开浏览器开发者工具（F12），查看 Console 中的错误信息

---

## 🗑️ 项目清理

当你完成测试，想要删除项目时：

### 第一步：停止运行的服务

1. **停止前端：** 在前端终端按 `Ctrl + C`，输入 `Y` 确认
2. **停止后端：** 在后端终端按 `Ctrl + C`，输入 `Y` 确认

---

### 第二步：删除 Docker 容器

```bash
# 停止并删除 Redis 容器
docker stop codesnippet-redis
docker rm codesnippet-redis

# 验证已删除
docker ps -a
```

---

### 第三步：删除数据库（可选）

**方法A：使用命令行**
```bash
mysql -u root -p
DROP DATABASE IF EXISTS codesnippet_db;
EXIT;
```

**方法B：使用 DataGrip**
1. 右键 `codesnippet_db`
2. 选择 "Drop Database"
3. 确认删除

---

### 第四步：删除项目文件夹

**Windows CMD：**
```bash
# 进入项目上级目录
cd D:\

# 删除项目文件夹
rmdir /s /q codesnippet-hub
```

**手动删除：**
直接在文件资源管理器中删除项目文件夹

---

## 📊 技术架构

### 技术栈

| 类型 | 技术 | 版本 | 说明 |
|------|------|------|------|
| **前端** | React | 18+ | UI 框架 |
| | Vite | 4+ | 构建工具 |
| | Tailwind CSS | 3+ | CSS 框架 |
| | CodeMirror 6 | 6+ | 代码编辑器 |
| **后端** | Node.js | 16+ | 运行环境 |
| | Express | 4+ | Web 框架 |
| | JWT | 9+ | 用户认证 |
| **数据库** | MySQL | 8+ | 主数据库 |
| | Redis | 7+ | 缓存 |
| **容器化** | Docker | 20+ | 容器化部署 |

---

### 系统架构图

```
┌─────────────────┐
│   用户浏览器     │
└────────┬────────┘
         │ HTTP
         ▼
┌─────────────────┐
│  React 前端      │ ← Vite Dev Server (5173)
│  (localhost:5173)│
└────────┬────────┘
         │ API 请求
         ▼
┌─────────────────┐
│  Express 后端    │ ← Node.js Server (3000)
│  (localhost:3000)│
└────┬────────┬───┘
     │        │
     ▼        ▼
┌─────────┐ ┌─────────┐
│  MySQL  │ │  Redis  │
│  (3306) │ │  (6379) │
└─────────┘ └─────────┘
```

---

### 项目目录结构

```
codesnippet-hub/
│
├── backend/                 # 后端代码
│   ├── config/             # 配置文件
│   │   ├── database.js     # MySQL 配置
│   │   └── redis.js        # Redis 配置
│   ├── controllers/        # 控制器（业务逻辑）
│   ├── models/             # 数据模型
│   ├── routes/             # API 路由
│   ├── middleware/         # 中间件
│   ├── server.js           # 服务器入口
│   └── package.json        # 依赖管理
│
├── frontend/               # 前端代码
│   ├── src/
│   │   ├── components/     # React 组件
│   │   ├── pages/          # 页面组件
│   │   ├── utils/          # 工具函数
│   │   ├── api/            # API 请求
│   │   └── App.jsx         # 应用入口
│   ├── index.html          # HTML 模板
│   ├── vite.config.js      # Vite 配置
│   └── package.json        # 依赖管理
│
├── README.md               # 项目说明
├── DEPLOYMENT_GUIDE.md     # 部署指南（本文档）
└── .gitignore              # Git 忽略文件
```

---

## 🎯 核心功能

### 用户认证
- ✅ 用户注册（邮箱 + 密码）
- ✅ 用户登录（JWT Token）
- ✅ 密码加密（bcrypt）
- ✅ 会话保持

### 代码片段管理
- ✅ 创建代码片段
- ✅ 编辑代码片段
- ✅ 删除代码片段
- ✅ 代码高亮显示（13种语言）
- ✅ 标签分类

### 社交互动
- ✅ 点赞功能
- ✅ 收藏功能
- ✅ 浏览量统计
- ✅ 用户主页

### 搜索与筛选
- ✅ 全文搜索
- ✅ 按语言筛选
- ✅ 按标签筛选
- ✅ 热门推荐

### 性能优化
- ✅ Redis 缓存（热门代码）
- ✅ 分页加载
- ✅ 图片懒加载

---

## 🔒 安全特性

### 认证与授权
- JWT Token 验证
- 密码 bcrypt 加密
- 中间件权限控制

### 数据验证
- 输入数据校验
- SQL 注入防护
- XSS 攻击防护

### 环境变量
建议在生产环境中使用 `.env` 文件存储敏感信息：

```env
# backend/.env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=codesnippet_db
JWT_SECRET=your_jwt_secret
REDIS_HOST=localhost
REDIS_PORT=6379
```

---

## 📈 性能指标

### 预期性能

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 首屏加载 | < 2s | 首次访问加载时间 |
| API 响应 | < 300ms | 平均接口响应时间 |
| 缓存命中率 | > 80% | Redis 缓存命中率 |
| 并发用户 | 100+ | 支持同时在线用户数 |

---

## 🚀 生产部署建议

如果你想将项目部署到生产环境：

### 推荐平台

1. **Vercel** - 前端部署（免费）
2. **Railway / Render** - 后端部署（免费额度）
3. **PlanetScale** - MySQL 托管（免费额度）
4. **Upstash** - Redis 托管（免费额度）

### 部署步骤（简化版）

1. 将代码推送到 GitHub
2. 在 Vercel 连接你的仓库（前端）
3. 在 Railway 连接你的仓库（后端）
4. 配置环境变量
5. 完成！

---

## 📚 学习资源

### 官方文档
- [React 官方文档](https://react.dev/)
- [Vite 官方文档](https://vitejs.dev/)
- [Express 官方文档](https://expressjs.com/)
- [MySQL 官方文档](https://dev.mysql.com/doc/)
- [Redis 官方文档](https://redis.io/documentation)

### 视频教程
- [Node.js 全栈开发教程](https://www.bilibili.com/)
- [React 入门到实战](https://www.bilibili.com/)
- [MySQL 数据库教程](https://www.bilibili.com/)

---

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

### 贡献流程

1. Fork 本仓库
2. 创建你的特性分支：`git checkout -b feature/AmazingFeature`
3. 提交你的更改：`git commit -m 'Add some AmazingFeature'`
4. 推送到分支：`git push origin feature/AmazingFeature`
5. 打开一个 Pull Request

---

## 📄 开源协议

本项目采用 MIT 协议开源。详见 [LICENSE](LICENSE) 文件。

---

## 💬 联系方式

- **GitHub：** [@michael123610](https://github.com/michael123610)
- **项目地址：** https://github.com/michael123610/codesnippet-hub
- **问题反馈：** [提交 Issue](https://github.com/michael123610/codesnippet-hub/issues)

---

## ⭐ 致谢

感谢所有为本项目做出贡献的开发者！

如果这个项目对你有帮助，请给我一个 ⭐️ Star！

---

<div align="center">

**Made with ❤️ by [michael123610](https://github.com/michael123610)**

🚀 **Happy Coding!** 🚀

</div>
